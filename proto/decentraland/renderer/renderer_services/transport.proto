syntax = "proto3";

package decentraland.renderer.renderer_services;

message Payload {
  bytes payload = 1;
}

service TransportService {
  // response and request are inverse on purpose
  rpc OpenTransportStream(stream Payload) returns (stream Payload) {}
}
