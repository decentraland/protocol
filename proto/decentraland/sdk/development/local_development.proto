syntax = "proto3";

package decentraland.sdk.development;

message UpdateScene {
  string id = 1;
  string type = 2;
}

message UpdateModel {
  string id = 1;
  string src = 2; // or hash ?
}

message WsSceneMessage {
  oneof message {
    // direction: scene->explorer
    UpdateScene update_scene = 1;
    UpdateModel update_model = 2;
  }
}
