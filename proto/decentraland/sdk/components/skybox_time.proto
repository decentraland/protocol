syntax = "proto3";
package decentraland.sdk.components;


import "decentraland/sdk/components/common/id.proto";

option (common.ecs_component_id) = 1210;

// wip
//
//
message PBSkyboxTime {
  float fixedTimeOfDay = 1;  // wip
  optional transitionMode transitionMode = 2;  // wip default is forward
}

// wip
//
//
enum TransitionMode {
  TM_FORWARD = 0;   // wip
  TM_BACKWARD = 1;  // wip
}