syntax = "proto3";

import "decentraland/bff/routing_service.proto";

package decentraland.kernel.comms.v3;

message Packet {
  string source = 1; // address
  map<string, decentraland.bff.PeerRoute> to = 2;
  bytes payload = 3;
}
