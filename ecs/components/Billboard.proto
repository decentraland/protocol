syntax = "proto3";

import "ecs/components/common/id.proto";
option (ecs_component_id) = 1090;

enum BillboardMode {
  AllAxes = 0;
  YAxe = 1;
}

message PBBillboard {
  optional BillboardMode billboardMode = 1; // default=AllAxes
  optional bool oppositeDirection = 2; // default=false
}